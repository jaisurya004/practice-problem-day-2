#include <stdio.h>

int main() {
    int i, n, temp = 0, count = 0;
    scanf("%d", & n);
    int a[n];
    for (i = 0; i < n; i++) {
        scanf("%d", & a[i]);
    }
    for(i=1;i<n;i++){
        count=0;
        for(int j=0;j<n;j++){
            if(i==a[j]){
            count++;
            break ;
            }
        }
        if(count==0)
            printf("%d ",i);
                    
    }
    return 0;
}
