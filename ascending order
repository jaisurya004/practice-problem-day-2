#include <stdio.h>

int main() {
    int i, n, temp = 0, min = 0;
    scanf("%d", & n);
    int a[n];
    for (i = 0; i < n; i++) {
        scanf("%d", & a[i]);
    }
    for(i=0;i<n;i++){
        for(int j=i+1;j<n;j++){
            if(a[i]>a[j]){
            temp=a[j];
            a[j]=a[i];
            a[i]=temp;
            }
        }
    printf("%d ",a[i]);
    }
    return 0;
}
